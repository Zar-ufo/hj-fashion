# Frontend environment template (safe to commit)
# Copy to frontend/.env.local and adjust if needed.

# Where the frontend proxies /api/* requests
BACKEND_URL="https://hj-fashion-3.onrender.com"

# Optional
NEXT_IGNORE_INCORRECT_LOCKFILE=1

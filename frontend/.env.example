# Frontend environment template (safe to commit)
# Copy to frontend/.env.local and adjust if needed.

# Where the frontend proxies /api/* requests
# URL of the deployed backend service (used by rewrites and server proxies)
# in production this should point at your backend Vercel deployment:
# e.g. https://h-jfashion0.vercel.app
BACKEND_URL="https://h-jfashion0.vercel.app"

# Optional
NEXT_IGNORE_INCORRECT_LOCKFILE=1

# Brevo email provider configuration (example)
BREVO_API_KEY="xsmtpsib-d1c29fad87f2ae63d890719617dc1f21a293806e087a2aad83f3b0e88055fdf0-0GK8AYjxAnn3zaXA"
BREVO_SENDER_NAME="HJ Fashion"
BREVO_SENDER_EMAIL="no-reply@poribortonfoundation.org"
